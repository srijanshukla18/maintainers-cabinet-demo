{
  "name": "cabinet-demo",
  "version": "2.1.1",
  "description": "Demo TypeScript config parser — sandbox repo for Maintainer's Cabinet",
  "main": "dist/index.js",
  "bin": { "cabinet-demo": "dist/cli.js" },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "eslint src tests"
  },
  "devDependencies": {
    "@types/jest": "^29",
    "@types/node": "^20",
    "jest": "^29",
    "ts-jest": "^29",
    "typescript": "^5"
  }
}
